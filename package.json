{
  "name": "supermom-crm",
  "version": "1.0.0",
  "description": "Supermom for Hire CRM",
  "scripts": {
    "start": "npx serve . -p 3000",
    "login": "clasp login",
    "push": "clasp push",
    "deploy": "clasp push && clasp deploy"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2",
    "purgecss": "^8.0.0"
  }
}
