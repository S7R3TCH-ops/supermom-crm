{
  "name": "supermom-crm",
  "version": "1.0.0",
  "description": "Supermom for Hire CRM",
  "scripts": {
    "login": "clasp login",
    "push": "clasp push",
    "deploy": "clasp push && clasp deploy"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  }
}